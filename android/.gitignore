# Android / Gradle
*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
app/build/
build/

# Keystore & signing secrets — NEVER commit (backed up in the private keystore repo)
*.jks
*.keystore
keystore.properties
secrets.txt

# kotlin daemon
.kotlin/
