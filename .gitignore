node_modules/
dist/
.DS_Store
*.log

# secrets & data — never commit
server/.env
server/data/
*.db
*.db-*

# build / editor
.vite/
.idea/
.vscode/

# Android app (android/) — build outputs + signing secrets (never commit)
android/.gradle/
android/.kotlin/
android/build/
android/app/build/
android/local.properties
android/captures/
*.jks
*.keystore
keystore.properties
secrets.txt
